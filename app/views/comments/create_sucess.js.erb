var $newComment = $("<%= j(render('/comments/comment', {comment: @comment})) %>")
$('#comments').prepend($newComment)

$newComment.hide().slideDown(1000);

<% @comment = Comment.new %>
$('#comment-form').html('<%= j render "form" %>')
